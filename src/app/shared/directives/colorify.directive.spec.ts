import { ColorifyDirective } from './colorify.directive';

describe('ColorifyDirective', () => {
  it('should create an instance', () => {
    const directive = new ColorifyDirective();
    expect(directive).toBeTruthy();
  });
});
