import { MagyverfyString.Pipe.TsPipe } from './magyverfy-string.pipe.ts.pipe';

describe('MagyverfyString.Pipe.TsPipe', () => {
  it('create an instance', () => {
    const pipe = new MagyverfyString.Pipe.TsPipe();
    expect(pipe).toBeTruthy();
  });
});
